/**
 * Site configuration — edit these values before sharing the site.
 * formUrl MUST be a full https:// URL (e.g. Google Forms link).
 */
const SITE_CONFIG = {
  // Paste your Google Form link here when ready:
  formUrl: 'https://forms.gle/2zZFt1VLwESCaXAH8',

  // WhatsApp: country code + number, no + or spaces (e.g. '919876543210')
  whatsapp: '',

  // Display label for WhatsApp button
  whatsappDisplay: 'Contact on WhatsApp'
};
